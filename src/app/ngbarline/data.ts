export interface data{
    data:{
    date: string,
    last_update: string,
    confirmed: Number,
    confirmed_diff: Number,
    deaths: Number,
    deaths_diff: Number,
    recovered: number,
    recovered_diff: Number,
    active: Number,
    active_diff: Number,
    fatality_rate: Number
    }
}